{
  "name": "{{packageName}}",
  "version": "0.0.1",
  "description": "Code Climate Collector: {{collectorSlug}}",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf ./lib/",
    "prepublishOnly": "{{packageManagerBin}} clean && {{packageManagerBin}} build && {{packageManagerBin}} test"
  },
  "files": [
    "lib/**/*",
    "README.md",
    "LICENSE"
  ],
  "main": "lib/index.js",
  "dependencies": {},
  "devDependencies": {}
}

