{
  "name": "create-codeclimate-connector",
  "version": "0.0.3",
  "description": "Scaffold a new Code Climate Connector",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf ./lib/",
    "prepublishOnly": "yarn clean && yarn build && yarn test"
  },
  "files": [
    "bin/**/*",
    "lib/**/*",
    "tmpl/**/*",
    "README.md",
    "LICENSE"
  ],
  "bin": "bin/create-codeclimate-connector",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^25.1.3",
    "@types/node": "^13.7.6",
    "jest": "^25.1.0",
    "ts-jest": "^25.2.1",
    "typescript": "^3.8.2"
  }
}
